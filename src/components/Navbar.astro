---

---

<!-- Desktop menu -->
<div class="hidden md:block">
    <nav class="flex justify-between items-center h-20">
        <ul class="list-none flex uppercase text-xs">
            <li class="mr-10"><a href="#">Můj glamping™</a></li>
            <li class="mr-10"><a href="#">Glampiny-house™</a></li>
            <li class="mr-10"><a href="#">O nás</a></li>
            <li class="mr-10"><a href="#">Kontakt</a></li>
        </ul>
    </nav>
</div>

<!-- Mobile menu -->
<div class="flex items-center md:hidden">
    <span id="toggle-menu" class="z-[99999] uppercase">Menu</span>
    <label class="relative z-[99999] cursor-pointer pl-2 py-6" for="mobile-menu">
        <input class="peer hidden" type="checkbox" id="mobile-menu" />
        <div class="relative z-[99999] block h-[2px] w-7 bg-black before:absolute before:-top-2 before:block before:h-[2px] before:w-full before:bg-black before:transition-all before:duration-500 before:ease-out before:content-[''] after:absolute after:top-2 after:block after:h-[2px] after:w-full after:bg-black after:transition-all after:duration-500 after:ease-out after:content-[''] peer-checked:bg-transparent before:peer-checked:top-0 before:peer-checked:rotate-45 before:peer-checked:transform after:peer-checked:top-0 after:peer-checked:-rotate-45 after:peer-checked:transform"></div>
    </label>
</div>

<!-- Fullscreen menu -->
<div id="fullscreen-menu">

    <div id="fullscreen-menu-wrapper" class="z-[9999] bg-dark-green-100 fixed h-screen w-screen top-0 right-0 transition duration-1000 opacity-0">

        <div class="grid grid-cols-6 px-5">
           <!-- Content column -->
           <div class="col-start-1 col-span-6 place-content-center">
               <!-- Navbar wrapper -->
               <ul class="list-none flex flex-col justify-center items-center gap-6 text-lg uppercase text-white">
                    <li><a href="#">Můj glamping™</a></li>
                    <li><a href="#">Glampiny-house™</a></li>
                    <li><a href="#">O nás</a></li>
                    <li><a href="#">Kontakt</a></li>
                    <li><a href="mailto:hello@glamping.cz">hello@glamping.cz</a></li>
                    <li><a href="tel:+420585451251">tel:+420585451251</a></li>
                </ul>
    
           </div>
       </div>


    </div>

</div>

<script>
    import { isOpen } from '../navbar.js';
  
    // Set the store to true when the button is clicked
    function openNavbar() {
        isOpen.set(!isOpen.get());
    }
  
    // Add an event listener to the button
    document.getElementById('mobile-menu').addEventListener('click', () => {
        const fullMenuWrapper = document.getElementById('fullscreen-menu-wrapper');
        const menuText = document.getElementById('toggle-menu');

        // Toggle the opacity of the fullscreen menu
        fullMenuWrapper.classList.toggle('opacity-100');

        // Toggle the text of the menu button
        if (fullMenuWrapper.classList.contains('opacity-100')) {
            menuText.innerHTML = "Zavřít"; // Change text to "Zavřít" when menu is open
        } else {
            menuText.innerHTML = "Menu"; // Change text back to "Menu" when menu is closed
        }

        // Toggle the state of the navbar
        openNavbar();
    });
</script>